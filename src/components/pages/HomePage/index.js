import React from 'react'
import { PageTemplate, AppBarMenu} from 'components'

const HomePage = () => {
  return (
    <PageTemplate>
      <AppBarMenu/>
    </PageTemplate>
  )
}

export default HomePage
